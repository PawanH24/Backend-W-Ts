//property interfaces

export enum Role {
  ADMIN = "ADMIN",
  USER = "USER",
  HOST = "HOST",
}
export enum PropertyType {
  Apartment = "Apartment",
  House = "House",
  Banglow = "Banglow",
}

export enum PriceType {
  PER_HOUR = "per_hour",
  PER_DAY = "per_day",
  PER_WEEK = "per_week",
  PER_MONTH = "per_month",
}
